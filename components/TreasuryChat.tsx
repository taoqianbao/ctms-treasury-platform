
import React from 'react';

// This component is deprecated and replaced by AIAgent.tsx
// Keeping file to prevent build errors if referenced elsewhere, but rendering nothing.

export const TreasuryChat: React.FC<any> = () => {
  return null;
};
